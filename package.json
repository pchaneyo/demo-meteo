{
  "name": "@cyklang/demo-weather",
  "version": "0.1.0",
  "scripts": {
    "start": "cyk start",
    "stop": "cyk stop",
    "dev": "nodemon --watch 'module/' --ext xml --exec 'cyk module u module/*.xml' "
  },
  "devDependencies": {
    "@cyklang/cli": "^0.9.0",
    "nodemon": "^3.0.1"
  }
}
